# ── Server ──────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=development

# ── Database ─────────────────────────────────────────────────────────────────
DB_PATH=./data/baseline.db

# ── OpenAI (topic analysis, claim trees, fact-check aggregation) ─────────────
OPENAI_API_KEY=sk-...

# ── Reddit API (divisiveness signal) ─────────────────────────────────────────
# Create an app at https://www.reddit.com/prefs/apps (script type)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=Baseline/1.0 (by /u/your_username)

# ── Google (Trends via SerpAPI — free tier: 100 searches/mo) ─────────────────
# Sign up at https://serpapi.com
SERPAPI_KEY=

# ── YouTube Data API v3 (free tier: 10,000 units/day) ────────────────────────
# Create at https://console.cloud.google.com → APIs → YouTube Data API v3
YOUTUBE_API_KEY=

# ── Polling sources (no keys needed — public RSS/HTML) ───────────────────────
# FiveThirtyEight, RealClearPolitics, Pew Research scraped directly

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,https://yourusername.github.io
